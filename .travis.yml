language: ruby
cache: bundler
services:
  - postgresql
before_script:
  - bundle exec rake db:setup
  - bundle exec rake db:test:prepare
script:
  - bundle exec rubocop
  - bundle exec rspec
notifications:
  email: false
